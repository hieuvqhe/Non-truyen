const authConfig = {
  baseURL: 'https://server-nontruyen.onrender.com/',
  tokenKey: 'auth_token', // for storing token in localStorage
  refreshTokenKey: 'refresh_token'
}

export default authConfig
